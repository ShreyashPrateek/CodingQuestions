class Result 
{
    public static long aVeryBigSum(List<Long> ar)
    {
    long sum = 0; // long is used because sum can be very large and int may not be able to hold it
    for(int i =0;i<ar.size();i++)
    {
        sum += ar.get(i);
    }
        return sum;
    }
}